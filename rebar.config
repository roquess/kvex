{erl_opts, [debug_info, warnings_as_errors]}.

{deps, []}.

{ct_opts, []}.

{edoc_opts, [
    {preprocess, true},
    {includes, ["include"]},
    {dir, "doc"}
]}.

{profiles, [
    {test, [{erl_opts, [nowarn_export_all]}]}
]}.
